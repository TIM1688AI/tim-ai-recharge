# 个人充值工作台

入口 `/admin`，默认关闭。现有公众页面保持原有布局；接口复用现有供应商适配器。工作台不购买卡密，不自动重试核销，不运行真实测试充值。

## 上线配置（Render + Cloudflare）

1. 创建与 Node 服务同区域的 PostgreSQL 数据库，选择满足业务的持久化、备份方案。将内部连接 URL 保存为 Render 的 `DATABASE_URL`，不要发到聊天或提交 Git。外部数据库连接需要按数据库证书要求启用 TLS，不能禁用证书验证。
2. 在自己的可信终端进入项目，执行 `npm ci` 和 `npm run admin:setup`。输入至少 14 字符密码（不回显）。工具只在本机输出密码哈希、TOTP 秘密和数据密钥；不要在 Render Build/日志中执行 setup。将 TOTP 秘密手动加入验证器（SHA-1、6 位、30 秒），并将密码、秘密和密钥安全备份。
3. 将下表变量保存到 Render；保留原有三个通道的配置，包括只在服务端使用的 `AGENT_API_KEY`。
4. 在设置了 `DATABASE_URL` 的受信任终端或 Render Shell 执行 `npm run admin:migrate`。迁移只创建带 `wb_` 前缀的表和索引，可重复执行，不删除原数据。数据库角色需具有建表权限；运行角色可单独限制为读写这些表。
5. 完成迁移后启用 `ADMIN_ENABLED=1` 和 `ADMIN_INVENTORY_GUARD=1`，重新部署。访问 `https://你的域名/admin` 登录。Cloudflare 不得缓存 `/admin*` 和 `/api-proxy/*`，不要配置 Cache Everything 覆盖服务端的 `no-store`。
6. 首先导入少量未售测试卡，检查别名、供应商产品与状态，再经人工确认做少量真实测试。真实核销会消耗卡密，不属于自动化测试。

Render 内部连接地址只能从其私有网络访问；如在本机执行迁移，需临时使用数据库提供的外部 TLS 连接地址，Render 服务仍保留内部连接配置。不要公开连接字符串。

| 变量 | 用途 |
| --- | --- |
| `ADMIN_ENABLED` | `1` 开放工作台页面和管理 API；默认关闭 |
| `ADMIN_INVENTORY_GUARD` | 上线后保持 `1`；即使关闭管理页面，也继续保护库存和恢复订单查询 |
| `ADMIN_ORIGIN` | 你实际访问的 HTTPS 根地址，如 `https://example.com`，不带路径；不使用通配符 |
| `DATABASE_URL` | PostgreSQL 连接字符串，仅服务端可见 |
| `ADMIN_PASSWORD_HASH` | setup 生成的 scrypt 哈希，不是明文密码 |
| `ADMIN_TOTP_SECRET` | setup 生成的验证器秘密 |
| `ADMIN_DATA_KEY` | setup 生成的 32 字节 Base64 数据密钥，必须独立备份，不能随意替换 |

生产不接受 HTTP 管理入口。仅本机非生产环境允许 `http://127.0.0.1:端口`。首次配置错误会导致启动失败；数据库或表不可用则管理操作及受保护核销返回错误，不降级为无锁充值。

**已有库存后不要同时关闭两个开关，也不要回滚到没有库存保护的旧版本。** 维护时只关闭 `ADMIN_ENABLED`，保留 `ADMIN_INVENTORY_GUARD=1`。更换 `ADMIN_DATA_KEY` 需要专门迁移，密钥不匹配时系统会拒绝管理和核销，避免旧卡被当成新卡。供应商地址与 `AGENT_API_KEY` 也不要在未完成订单处理中随意更换，高阶幂等号与现有供应商密钥有关。

## 入库、发卡、充值

- 每次最多 500 张、100 KB，支持单列 TXT/CSV（可带 `card`/`code`/`卡密` 表头），先预览再入库。无效行逐行反馈，其他行可入库。
- `JZ-/JZ5X-/JZ20X-` 对应 `TIM-/TIM5X-/TIM20X-`，11 位后缀不变；16 位年度卡不改。
- `CLAUDEPRO-/CLAUDEMAX5-/CLAUDEMAX20-` 对应 `TIMC-PRO-/TIMC-MAX5-/TIMC-MAX20-`。
- `PLUS-/PRO5-/PRO20-` 对应 `TIMG-PLUS-/TIMG-PRO5-/TIMG-PRO20-`。常规卡保持原样。
- 原始与 TIM 别名统一去重；不接受旧 LZ，也不猜测未知格式。后缀严格校验，不静默改写大小写。
- 新卡先待验证；供应商确认可用且产品匹配后才进入库存。进阶还须返回未使用，高阶还须确认未关联旧订单。未知产品名称保留待验证，不凭前缀替代供应商产品确认；如遇供应商新增名称，需要审查后补充 `card-catalog.js` 的明确映射。
- 历史已经发出的卡入库时必须勾选“已发给客户”。导入不会替你识别未核销但已出售的卡。
- 发卡每批 1–100 张，先原子出库，再下载 TIM 格式文件。失败时可从批次重新下载；同一请求不会重复出库。原始全卡不对外输出。
- 工作台逐个账号充值，数据库原子占用卡密和创建订单。选择哪条通道就使用哪条通道，不跨通道替代。
- 高阶 GPT 的 Session 只在浏览器提取 `account.id`，提交 ID；常规和进阶 Session 仅用于当次处理。数据库不存 Session、访问令牌、密码或供应商完整响应。
- 公众入口和合作方 API 的核销同样经过库存保护。可用库中的卡不能从公众入口核销；已出库卡可以核销并形成订单。启用保护后，未入库卡也保留不可逆指纹占用，防止其在请求执行中又被导入库存；结果不明请查询，不重发。
- 公众入口对工作台管理卡的换卡/取消被拒绝，以免绕过库存状态。

## 异常处理与数据

订单状态：待处理、处理中、成功、失败、待确认。库存状态：待验证、可用、已占用、已出库、已核销、已隔离。

进程每 30 秒领取最多 5 个到期查询任务；原订单只查询不重发。正常查询间隔约 2 分钟，失败约 5 分钟。服务休眠期间不运行后台查询，唤醒后继续；需要持续更新时使用不会休眠的服务配置。

未知状态不自动失败、不自动换卡、不解除占用。明确失败的卡隔离；只有原订单已终结、未消耗且供应商重新验证可用时才能手动回库。高阶仍关联旧幂等订单的卡不会回库，需先让供应商处理；不生成新幂等号绕过旧订单。无法确认的订单保留待确认并联系供应商，本版不提供任意修改“成功”的按钮。

完成时间优先使用供应商时间；未返回有效时间则采用本站确认时间并在订单备注标明。因此跨天补查且缺少供应商时间时，不能宣称精确还原供应商的完成日期。任务编号冲突、完成时间明显早于本次订单保持待确认。

记录按北京时间检索，单次不超过一年/一万条，表格每页 100 条，CSV 导出按筛选条件。邮箱可选；手动 ID 不会自动推算邮箱，供应商返回的脱敏线索不当作真实完整邮箱。成功账号数按日期、通道、产品分别去重，不能把不同分组直接相加为全站去重人数。

敏感查看和导出需最近 5 分钟内验证身份，登录时算一次验证。密码与动态验证码再次校验；同一 TOTP 不能重复使用。登录有效 8 小时，Cookie 使用 HttpOnly/SameSite，HTTPS 下为 Secure。日志不输出连接字符串、令牌、完整卡密、账号或请求体；Cloudflare、Render 及额外 APM 也必须关闭这些内容的采集。

本站记录需向客户告知用于履约及售后，公众页文案已避免“不留存账号信息”的不实承诺。数据默认保留供分析；按你的隐私要求制定保留/删除周期，备份也须同步执行。不要把数据库备份与加密密钥存放在同一公开位置。定期验证数据库备份和独立密钥能够恢复。

## 测试与边界

`npm test` 包含原网站回归测试和工作台测试。默认使用 PGlite 的 PostgreSQL 引擎执行真实 SQL，但只模拟单连接池调度；不能代替部署环境的多连接/多实例锁竞争验收。可在**专用测试库**设置 `TEST_DATABASE_URL` 执行 PostgreSQL 测试（建立随机 `wb_test_` schema，结束后仅删除该测试 schema），切勿指向生产库。

测试供应商完全模拟，不访问真实充值接口。测试包括转换、重复入库、出库幂等、重复点击、Session 不落库、公众入口防争用、未知状态保留、重启查询、登录/CSRF/TOTP、导出鉴权。

新依赖 `pg` 为运行依赖，`@electric-sql/pglite` 仅用于开发测试；生产使用 `npm ci --omit=dev`。数据库备份、生产密钥配置和真实测试充值必须由管理员在上线阶段完成。
